[project]
name = "artifact-fm-qsfo"
version = "0.1.0"
description = "Artifact for monitoring quantitative signal first-order logic"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "lark>=1.3.1",
    "polars>=1.38.1",
    "pplpy>=0.8.10",
    "sympy>=1.14.0",
]
